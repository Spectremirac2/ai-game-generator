# Example environment configuration for ai-game-generator (safe to commit)
# Copy this file to `.env.local` and replace the placeholder values.

# OpenAI API key from https://platform.openai.com/account/api-keys
OPENAI_API_KEY=sk-openai-example-key

# Vercel Postgres connection strings from the Vercel dashboard (Storage > Postgres)
POSTGRES_URL="postgresql://user:password@vercel-postgres-host:5432/vercel_db"
POSTGRES_PRISMA_URL="postgresql://user:password@vercel-postgres-host:5432/vercel_db?schema=public"
POSTGRES_URL_NON_POOLING="postgresql://user:password@vercel-postgres-host:5432/vercel_db"

# Upstash Redis REST credentials from https://console.upstash.com/redis
UPSTASH_REDIS_REST_URL="https://live-dodo-19482.upstash.io"
UPSTASH_REDIS_REST_TOKEN="AUwaAAIncDI1NmQ4NTMzZWVhZmE0MjA3YjZmMzIzNWRiNjE5ZDA2NXAyMTk0ODI"

# NextAuth secrets and OAuth credentials
# Generate AUTH_SECRET with `openssl rand -base64 32`
AUTH_SECRET="example-auth-secret"
# GitHub OAuth App credentials from https://github.com/settings/developers
AUTH_GITHUB_ID="github-client-id"
AUTH_GITHUB_SECRET="github-client-secret"
# Google OAuth client credentials from https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID="google-client-id.apps.googleusercontent.com"
AUTH_GOOGLE_SECRET="google-client-secret"
# Base URL of your app; update to production URL when deploying
AUTH_URL="http://localhost:3000"

# Public application settings
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"

# Rate limiting thresholds (adjust as needed)
RATE_LIMIT_FREE_TIER=10
RATE_LIMIT_PRO_TIER=100

# Secret for securing cron job endpoints; generate with `openssl rand -hex 32`
CRON_SECRET="example-cron-secret"
